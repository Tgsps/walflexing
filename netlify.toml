[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so react-router deep links work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
